export type FormButtonsProps = {
    type: "button" | "submit" | "reset";
};

export const formButtons: FormButtonsProps[] = [
    {
        type: "submit",
    },
    {
        type: "reset",
    },
];

import s from "./Logo.module.scss";

const Logo = () => {
    return (
        <div className={s.logo}>
            <div />
            <div />
        </div>
    );
};

export default Logo;
